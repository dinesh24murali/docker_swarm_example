version: "3.9"

services:
  node-app:
    build:
      context: .
      args:
        NODE_ENV: production
    volumes:
      - ./:/app
      - /node_modules
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    command: ["npm", "run", "start"]

